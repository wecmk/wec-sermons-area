<form method="post" action="{{ path('series_delete', {'id': series.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ series.id) }}">
    <button class="btn">Delete</button>
</form>
