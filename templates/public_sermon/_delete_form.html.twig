<form method="post" action="{{ path('public_sermon_delete', {'id': public_sermon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ public_sermon.id) }}">
    <button class="btn">Delete</button>
</form>
